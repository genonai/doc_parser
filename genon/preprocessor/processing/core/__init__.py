"""고객이 열지 않는 처리 본체.

`facade/preprocessor_template.py`(고객은 `preprocessor.py` 로 복사해 쓴다)가 여기의
`ParserCore` / `ChunkerCore` 를 상속하고, 사이트마다 달라지는 것만 얹는다.
계획: docs/plans/facade-slimming/08-customer-facade.md
"""
