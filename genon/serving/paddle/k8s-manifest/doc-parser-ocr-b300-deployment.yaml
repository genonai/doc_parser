apiVersion: apps/v1
kind: Deployment
metadata:
  name: doc-parser-ocr-b300-deployment
  namespace: llmops
  labels:
    app: doc-parser-ocr-b300
    variant: b300
spec:
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: doc-parser-ocr-b300
  template:
    metadata:
      labels:
        app: doc-parser-ocr-b300
        variant: b300
    spec:
      serviceAccountName: llmops
      containers:
        - name: doc-parser-ocr-b300
          image: mncregistry:30500/doc-parser-ocr-b300:0.0.0
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: TZ
              value: Asia/Seoul
            - name: PROFILE
              value: prod
          volumeMounts:
            - name: nfs-root
              mountPath: /nfs-root
          resources:
            limits:
              nvidia.com/gpu: 1
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 15
      volumes:
        - name: nfs-root
          persistentVolumeClaim:
            claimName: llmops-nfs-root
        - name: license-volume
          secret:
            secretName: llmops-license
        - name: global-configmap
          configMap:
            name: llmops-global-configmap
---
apiVersion: v1
kind: Service
metadata:
  name: doc-parser-ocr-b300-service
  namespace: llmops
  labels:
    app: doc-parser-ocr-b300
spec:
  selector:
    app: doc-parser-ocr-b300
  ports:
    - name: http
      port: 8080
      targetPort: 8080
