# build.config
# 도커 레지스트리
DOCKER_REGISTRY=mncregistry:30500

# 이미지 이름
IMAGE_NAME=doc-parser-preprocessor

# 버전 (git tag, 브랜치 이름, 날짜 등으로 교체 가능)
IMAGE_VERSION=1.3.6.3

# 실제 Dockerfile 위치 (루트 기준)
DOCKERFILE_PATH=genon/preprocessor/docker/Dockerfile

# 빌드 후 push 할지 여부
PUSH_IMAGE=true

# SDK 아티팩트(hwp_sdk / pdf_sdk)를 매 빌드마다 HF에서 새로 받을지 여부.
#  true  : sdk_artifacts / pdf_sdk_artifacts 스테이지를 캐시 무시하고 재실행 (SDK가 항상 최신, 권장)
#  false : Docker 레이어 캐시 사용 — SDK가 HF에서 갱신돼도 빌드에 반영 안 될 수 있음
SDK_NO_CACHE=true

# USER, GROUP
APP_UID=3000
APP_GID=3000
APP_UNAME=genos
APP_GNAME=genos

# NLTK packages (comma-separated). Use "all" to download everything.
APP_NLTK_PACKAGES=punkt,stopwords,averaged_perceptron_tagger,averaged_perceptron_tagger_eng,wordnet,omw-1.4

# HuggingFace Access Token (Private Repo 접근용)
# ⚠️  여기에 직접 입력하지 말 것 — 토큰은 반드시 hf_private_token.env 파일에만 설정하세요. (Git에 커밋되지 않음)
# 예시: echo "HF_TOKEN=hf_your_token_here" > build-script/hf_private_token.env
HF_TOKEN=SET_IN_hf_private_token.env