# Repository-specific Codex defaults. Project config is loaded only for trusted repositories.
project_doc_fallback_filenames = ["CLAUDE.md"]

# Inherit the model and reasoning effort from the user's global Codex settings.
model_verbosity = "low"
